# Unused air-operation sketches

These modules are preserved design work, not executable game imports. Their relative import paths assume eventual placement in `game/src/`. Daylight/weather balance and aircraft compatibility still need integration, validation and the corresponding catalog review before use.
