# Session checkpoint — 12 September 2026

Completed runtime checkpoint: **0.18.2**, save format **10**. Start a new campaign. The 0.18.1 private GitHub release and Windows CI build completed successfully before this checkpoint.

Completed changes: scrolling naval-command list; stable map-marker offsets; concealed outbreak dates in war warnings; monthly real-output growth affecting gold and industry; actual monthly reserve changes and growth inputs in resource hovers; monthly civilian merchant deliveries; civilian ships removed from naval construction; 1922/1932/1942 support generations; Japan's In Good Faith Standard Maru tender as oiler/depot hybrid. All 28 generated national Markdown catalogs match the current game data.

The movement audit found no coordinate discontinuities in a seven-day all-navy trace and in regression checks spanning both campaign openings. Marker offsets are now stable across redraws. This addresses display relocations; it does not prove every possible later campaign route is free of defects.

The full catalog review is `catalog-14-starts.html`. The user confirmed **three-year generations for all aircraft**, except the preserved **Japan 1936 As Long As It's Black** program. Japan 1922 changes normally. Those two questions are answered; do not ask them again.

Unfinished: operational air warfare (daylight/weather, search sectors, strike assembly, deck cycles, explicit CAP/escort allocation, anchorage reconnaissance), revised naval aircraft roster, passive government-managed maritime aircraft and strict carrier/floatplane/ferry compatibility. These are **not active in the portable build**. The unvalidated candidate data are preserved in `aircraft-generation-proposal.json`; authoring logic is in `tools/revise-aircraft-catalogs.mjs`. The candidate uses representative role/period labels, not a fully researched historical model list. Review and improve those model choices before activating the catalog. `air-prototype/` contains unused initial conditions/compatibility sketches; no game modules import them.

Strategic bombing recommendation: a later government-run, abstract weekly air campaign in Land campaigns, with bomber/escort/defense strength, weather, temporary industry disruption and repairs. Keep its accounting separate from maritime aircraft and avoid charging the same shipping/industrial loss twice. This concept is recommended, not implemented or approved for implementation.

Validation approach: 193 applicable regression checks passed across the main run and the focused rerun after updating obsolete merchant-order/support-generation expectations. Old-save migration tests and unbounded multi-decade tests are intentionally excluded. Use the documented `test:release` selection; do not run the unfiltered suite. Finish with the packaged executable checks for both starts, not only browser-source tests.

Keep this continuation bounded: first finalize the concrete aircraft catalog, then wire one small automatic sortie state machine with conservation/range checks, then make one new portable build. No extra user-facing sortie controls are requested. No store publishing or public-source release has been authorized.
